import PendingLicenseContent from "@/components/Dashboard/pending-request/PendingLicenseContent";

const PendingRequest = () => {
  return <PendingLicenseContent type="event_planner" />;
};

export default PendingRequest;
